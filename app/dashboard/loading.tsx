/* app/dashboard/loading.tsx — full-shell skeleton while the dashboard loads. */
import { ShellSkeleton } from "@/components/shell/loading-skeleton";

export default function DashboardLoading() {
  return <ShellSkeleton />;
}
