/* app/classes/[id]/loading.tsx — content skeleton (renders inside the class shell). */
import { ContentSkeleton } from "@/components/shell/loading-skeleton";

export default function ClassLoading() {
  return <ContentSkeleton />;
}
