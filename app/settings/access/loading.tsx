/* app/settings/access/loading.tsx — full-shell skeleton while settings load. */
import { ShellSkeleton } from "@/components/shell/loading-skeleton";

export default function SettingsLoading() {
  return <ShellSkeleton />;
}
